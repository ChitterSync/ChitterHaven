export {
  AUTH_COOKIE_NAME,
  getAuthCookieOptions,
  setAuthCookie,
  clearAuthCookie,
  getAuthCookie,
} from "@/server/api-lib/authCookie";

export default function handler() {
  // Internal helper module; not intended to be called as a route.
}
