const KEYS=/authorization|cookie|token|password|hash|pepper|secret|key|migration|sdp|ice|turn|message|attachment|recovery/i;const TOKENS=/(?:Bearer\s+)?(?:eyJ[A-Za-z0-9_-]{10,}\.[A-Za-z0-9_-]{10,}\.[A-Za-z0-9_-]{10,}|[A-Za-z0-9_-]{32,})/g;
export const redact=(value:unknown,seen=new WeakSet<object>()):unknown=>{if(typeof value==="string")return value.replace(TOKENS,"[REDACTED]");if(!value||typeof value!=="object")return value;if(seen.has(value))return"[CIRCULAR]";seen.add(value);if(value instanceof Error)return{name:value.name,message:redact(value.message)};if(Array.isArray(value))return value.map(v=>redact(v,seen));return Object.fromEntries(Object.entries(value as Record<string,unknown>).map(([k,v])=>[k,KEYS.test(k)?"[REDACTED]":redact(v,seen)]));};
